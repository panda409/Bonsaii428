@model IEnumerable<BonsaiiModels.StaffVariation.StaffVariation>

@{
    ViewBag.Title = "变更记录";
}

<div class="control-bar card bor-b">
    <ul class="breadcrumb pull-left pv">
        <li><a href="#"><i class="fa fa-home"></i>主页</a></li>
        <li><a href="#">人事管理</a></li>
        <li><a href="#">@ViewBag.Title</a></li>
        <li><a href="#">@ViewBag.name</a></li>
       
    </ul>
    <div class="h-list pull-right pv">
        @*<a href="/StaffChange/Create"><i class="fa fa-plus"></i>新增</a>*@
        <a href="#" id="refresh"><i class="fa fa-refresh"></i>刷新</a>
        <a href="#"><i class="fa fa-print"></i>输出</a>

    </div>
</div>


<div class="card p">


    <div class="main-table mt">

        <table class="table table-striped table-hover table-bordered display " id="example" cellspacing="0">
                <thead>
                    <tr>
                        <th class="checkbox-wrap">
                            <input type="checkbox" id="t1"><label for="t1"></label>
                        </th>
                        <th class="sorting_disabled">
                            序号
                        </th>
                        <th>
                            @Html.DisplayNameFor(model => model.FieldName)
                        </th>
                        <th>
                            @Html.DisplayNameFor(model => model.FieldValue)
                        </th>
                        <th>
                            @Html.DisplayNameFor(model => model.VariationRecordTime)
                        </th>
                        @*<th>
                            @Html.DisplayNameFor(model => model.VariationEffectTime)
                        </th>
                        <th>
                            @Html.DisplayNameFor(model => model.Initial)
                        </th>*@
                        @*<th>操作</th>*@
                    </tr>
                </thead>

                @{
                    int index = 0;
                }
                <tbody>
                    @foreach (var item in Model)
                    {
                        index = index + 1;
                        <tr>
                            <td class="checkbox-wrap"><input type="checkbox" id="item_@index"><label for="item_@index"></label></td>
                            <td>
                                @index
                            </td>

                            <td>
                                @Html.DisplayFor(modelItem => item.FieldName)
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item.FieldValue)
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item.VariationRecordTime)
                            </td>
                            @*<td>
                                @Html.DisplayFor(modelItem => item.VariationEffectTime)
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item.Initial)
                            </td>*@

                            @*<td>*@
                            @*@Html.ActionLink("编辑", "Edit", new { id = item.Id }) |
                            @Html.ActionLink("查看", "Details", new { id = item.Id }) |
                            @Html.ActionLink("删除", "Delete", new { id = item.Id })|
                            @Html.ActionLink("查看变更信息", "Look", new { staffChangeId = item.Id, id = item.StaffNumber })*@
                            @*@Html.ActionLink("查看变更信息", "Look", new { staffChangeId = item.Id, id = item.StaffNumber })*@
                            @*<a href="/StaffChange/Details/@item.Id" class="icon fa fa-search bgc-blue-500"></a>*@

                            @*<a href="/StaffChange/Look?id=@item.StaffNumber&staffChangeId=@item.Id" class="icon fa fa-list-ol bgc-blue-500" title="变更信息"></a>
                            <a href="/StaffChange/Edit/@item.Id" class="icon fa fa-edit bgc-blue-500" title="编辑"></a>
                            <a href="/StaffChange/Details/@item.Id" class="icon fa fa-search bgc-blue-500" title="详情"></a>
                            <a href="/StaffChange/Delete/@item.Id" class="icon fa fa-trash bgc-red-500" title="删除"></a>*@

                            @*<span class="icon fa fa-search bgc-blue-500"></span>@Html.ActionLink("查看", "Details", new { id = item.Id }) |
                            <span class="icon fa fa-edit bgc-blue-500"></span>@Html.ActionLink("编辑", "Edit", new { id = item.Id }) |
                            <span class="icon fa fa-trash bgc-red-500"></span>@Html.ActionLink("删除", "Delete", new { id = item.Id })*@
                            @*@if (item.AuditStatus == 0)*@
                            @*@*{*@
                            @*<a href="/StaffChange/Submit/@item.Id" class="icon fa fa-hand-o-up bgc-blue-500"></a>*@
                            @*<span class="icon fa fa-hand-o-up bgc-blue-500"></span>@Html.ActionLink("提交", "Submit", new { id = item.Id })*@
                            @*}*@
                            @*@if (item.AuditStatus == 6)
                            {
                                var str = "|";
                                @str
                                <span class="glyphicon glyphicon-hand-up"></span>@Html.ActionLink("通过", "ManualAudit", new { id = item.Id, flag = 1 })

                                @str

                                <
                                <span class="glyphicon glyphicon-hand-down"></span>@Html.ActionLink("不通过", "ManualAudit", new { id = item.Id, flag = 0 })

                            }*@

                            @*</td>*@
                        </tr>
                    }

                    @{
                        int num = Model.Count();

                    }
                </tbody>
            </table>


</div>



</div>
<script>

        //$("#upchevron").click(function () {
        //    if ($("#upspan").attr("class") == "glyphicon glyphicon-chevron-up") {
        //        $('#upspan').prop('class', "glyphicon glyphicon-chevron-down");
        //    }
        //    else {
        //        $('#upspan').prop('class', "glyphicon glyphicon-chevron-up");
        //    }
        //});
        $("#add").click(function () {
        window.location.href = "/StaffChange/Create"
        });

    $("#refresh").click(function(){
    window.location.reload();
    });

    $("#rootcheckbox").change(
        function () {
            if ($('#rootcheckbox').is(":checked")) {
                $('.brancheckbox').prop('checked', true);
            }

            else
                // $('.brancheckbox').attr("checked",false);//
                $('.brancheckbox').removeAttr("checked");
        }
        );

    $(".Id").click(function () {
        attr("yes");

    }
    );

</script>

